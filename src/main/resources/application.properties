
spring.application.name=authsystem
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://aws-1-eu-west-3.pooler.supabase.com:6543/postgres?prepareThreshold=0}
spring.datasource.username=${DATABASE_USERNAME:postgres.lfbzlsligrrzyujndjfn}
spring.datasource.password=${DATABASE_PASSWORD:59SYuLdYY48mBBYZ}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.jdbc.batch_size=0

# Server port (Render sets PORT env variable)
server.port=${PORT:8080}

# JWT Configuration
app.jwt.secret=${JWT_SECRET:4r5t6y7u8i9o0p1q2w3e4r5t6y7u8i9o0p1q2w3e4r5t6y7u8i9o0p1q2w3e4r5}
app.jwt.expiration=86400000
   
# SMTP Configuration
spring.mail.host=${SMTP_HOST:mail.niishmail.com}
spring.mail.port=${SMTP_PORT:587}
spring.mail.username=${SMTP_USERNAME:noreply@deskmayte.com}
spring.mail.password=${SMTP_PASSWORD:zSEg6uRnE58Matc}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
unosend.from.email=${SMTP_FROM:noreply@deskmayte.com}
